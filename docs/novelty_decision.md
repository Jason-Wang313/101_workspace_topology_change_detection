# Novelty Decision

Decision: conditionally promising.

The v4.1 continuation evidence supports keeping the idea alive as STRONG_REVISE. The mechanism is more specific than generic scene-graph change detection because the topology graph is action-conditioned and tied to invalid-plan prevention.

The novelty is not enough for ICLR main without external validation and a deeper related-work survey.
